# ===================================================================
# NATIVEPHP MOBILE/DESKTOP APP BUILD ENVIRONMENT
# ===================================================================
# This .env configuration is ONLY for building NativePHP mobile/desktop apps
# (Capacitor, Tauri, Electron) that need to call remote production API
#
# KEY: NATIVE_APP=true to enable cross-origin API calls
# ===================================================================

APP_NAME='سؤال محيرني'
APP_ENV=production
APP_KEY=base64:cxxGpzG0YUbVqYSNMcPbBuLt4z0R8Rr7gBG9SB8LDFA=
APP_DEBUG=false
APP_URL=https://so2al.free.nf

# ===================================================================
# CRITICAL: Set NATIVE_APP=true for mobile/desktop builds
# This tells the frontend to make cross-origin calls to REMOTE_APP_URL
# ===================================================================
NATIVE_APP=true

# ===================================================================
# REMOTE_APP_URL: Production API server URL
# All API calls will be prefixed with this URL
# Example: apiGet('/api/native/questions') → https://so2al.free.nf/api/native/questions
# ===================================================================
REMOTE_APP_URL=https://so2al.free.nf

# ===================================================================
# NATIVE_APP_ALLOWED_ORIGINS: Origins that are allowed to call the API
# This is configured on the SERVER side (production Laravel)
# Include capacitor://, tauri://, and any other app protocols
# ===================================================================
NATIVE_APP_ALLOWED_ORIGINS=capacitor://localhost,tauri://localhost,app://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=info

# ===================================================================
# Database: Local SQLite for offline storage
# Mobile/desktop apps use local DB for caching, not for main data
# ===================================================================
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

SESSION_DRIVER=file
SESSION_LIFETIME=120

CACHE_STORE=file
QUEUE_CONNECTION=sync

VITE_APP_NAME="${APP_NAME}"

# ===================================================================
# NativePHP Configuration
# ===================================================================
NATIVEPHP_APP_ID=com.minahawarei.so2al_mehairny
NATIVEPHP_APP_VERSION=1.0.0
NATIVEPHP_APP_VERSION_CODE=2

# Android Keystore Configuration
ANDROID_KEYSTORE_FILE=credentials/soual-mehairny.jks
ANDROID_KEYSTORE_PASSWORD=968420
ANDROID_KEY_ALIAS=soual-mehairny
ANDROID_KEY_PASSWORD=968420
